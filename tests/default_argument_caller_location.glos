where :: (message := "", location := #caller_location) {
    print(location, "HERE")
    if message != "" {
        print(":", message)
    }
    println()
}

main :: () {
    where()
    where("Additional message!")
}
