Vec2 :: struct {
    x: s32
    y: s32
}

impl Vec2 for Add {
    add :: (lhs: Self, rhs: Self) -> Self {
        return { lhs.x + rhs.x, lhs.y + rhs.y }
    }
}

main :: () {
    v: Vec2 = {34, 200} + {35, 220}
    println(v)
}
