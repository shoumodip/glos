Vec2 :: struct {
    x: s32
    y: s32
}

operator + :: (this: Vec2, that: Vec2) -> Vec2 {
    return { this.x + that.x, this.y + that.y }
}

main :: () {
    v: Vec2 = {34, 200} + {35, 220}
    println(v)
}
