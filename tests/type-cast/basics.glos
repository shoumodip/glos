fn printInt(x i64)   { print x }
fn printBool(x bool) { print x }

fn main() {
    var a &&i64 = <&&i64> 0
    var b &bool = <&bool> a

    printInt(<i64> true)
    printInt(<i64> false)

    printBool(<bool> -69)
    printBool(<bool> 0)
    printBool(<bool> 420)
}
