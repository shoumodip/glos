main :: () {
    foo :: (x: $T) {
        bar :: (x: s64) {
            baz :: (y: $A, z: $B) {
                println("\{y} \{z}")
            }

            baz(x, "Urmom")
        }

        bar(x)
    }

    foo(69)
}
