EXIT 1
STDOUT 0

STDERR 353
tests/error_cannot_use_interpolated_string_here.glos:2:5: ERROR: Cannot use interpolated strings here

    "foobar \{69}"

    Interpolated strings are compiled to a temporary '[]Any' value, and can only be used like this:

        bar :: (vs: []Any) {}
        foo :: (vs: ...Any) {}

        foo("Nice: \{34 + 35}")
        bar("Nice: \{200 + 220}")


