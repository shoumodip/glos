Foo :: enum {
    x = 34
    y = 35
    z = 420
    w = 1757
}

foo :: () -> Foo, Foo {
    return .x + .y, .z
}

bar :: (a: Foo) {
    println(i64(a))
}

main :: () {
    a, b := foo()
    println(i64(a))
    println(i64(b))
    bar(.w - .z)
    assert(a == .x + .y)
}
