Show :: union {
    s64
    string
}

show :: (s: Show) {
    println(s.type)

    if s == {
    case s64;    println(s)
    case string; println(s)
    case;        println("nil")
    }
}

main :: () {
    s: Show
    show(s)
    show(Show(69))
    show(Show("Urmom"))

    s = Show(420)
    if s == s64 {
        assert(s == 420)
    }
}
