main :: () {
    foo :: (xs: [$N]s64) {
        println("foo:")
        println(xs)
        println("N = \{N}")
    }

    bar :: (xs: [$N]$T) {
        println("bar:")
        println(xs)
        println("N = \{N}")
        println("T = \{T}")
    }

    foo([]s64 {69, 420, 1337})
    bar([]string {"Deez", "Nuts"})
}
