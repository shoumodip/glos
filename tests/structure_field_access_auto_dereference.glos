Iota :: struct {
    x: s64
}

single :: (i: &Iota) {
    i.x = i.x + 1
}

double :: (i: &&Iota) {
    i.x = i.x + 1
}

main :: () {
    iota := Iota {x = 67}
    ptr := &iota
    println(iota.x)

    single(ptr)
    println(iota.x)

    double(&ptr)
    println(iota.x)
}
