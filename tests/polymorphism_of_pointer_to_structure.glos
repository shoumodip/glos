Box :: struct($T: Type) {
    v: T
}

show :: (b: &Box($T)) {
    println(*b)
}

main :: () {
    b: Box(i64) = {69}
    p: &Box(i64) = &b
    show(&b)
    show(p)

    b: Box(string) = {"Urmom"}
    p: &Box(string) = &b
    show(&b)
    show(p)
}
