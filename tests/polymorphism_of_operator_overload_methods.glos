Array :: struct($T: Type, $N: i64) {
    items: [N]T
}

index :: (this: &Array($T, $N), n: i64, assign: bool) -> &T {
    assert(n < this.items.count)
    return &this.items[n]
}

main :: () {
    xs: Array(i64, 4)
    xs[0] = 69
    xs[1] = 420
    xs[2] = 1337
    println(xs)
}
